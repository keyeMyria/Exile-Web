version: '2'
services:   
    http-server:
        build: .
        ports:
            - "8888:8080"
        volumes:
            - .:/exileweb
        command: bash -c "ng build --prod &&  http-server ./dist"
        
